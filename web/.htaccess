<IfModule mod_rewrite.c>
    Options +FollowSymLinks
    Options -Indexes
    RewriteEngine	on
    RewriteCond     %{SCRIPT_FILENAME}  !-f
    RewriteCond $1 !^(robots\.txt|img|css|js)
    RewriteRule		^(.+) index.php [L]
</IfModule>
